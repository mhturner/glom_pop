from glom_pop import util
import inspect
import os


os.path.join(inspect.getfile(util).split('glom_pop')[0], 'glom_pop', 'config.yaml')

inspect.getfile(util).split('glom_pop')[0]

inspect.getfile(dataio)
